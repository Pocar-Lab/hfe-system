# Firmware Include Directory

This directory is reserved for project header files shared by firmware source
files under `firmware/src/`.

The current controller firmware is implemented in `firmware/src/main.cpp`, so
there are no project headers here yet. Add headers here only when declarations
need to be shared across multiple firmware translation units.
